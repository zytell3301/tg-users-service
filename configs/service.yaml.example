# This is the ip of the server that will be used for several
# purposes like grpc server ip and etc.
node-ip:

# A unique id that indicates
service-id:

instance-id:

# Service port is used for grpc server
service-port:

# This will be used for generating some king of uuids like v5
uuid-space: